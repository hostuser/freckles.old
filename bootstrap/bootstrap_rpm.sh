#!/usr/bin/env bash

# export DEBIAN_FRONTEND=noninteractive
# set -e

# install (minimal) required packages
# sudo apt-get update
# sudo apt-get install -y build-essential git python-dev python-virtualenv libssl-dev libffi-dev stow

# create freckles virtualenv
# FRECKLES_DIR="$HOME/.freckles"
# FRECKLES_VIRTUALENV="$FRECKLES_DIR/virtualenv"

# echo "$FRECKLES_DIR"
# mkdir -p "$FRECKLES_DIR"
# export WORKON_HOME="$FRECKLES_VIRTUALENV"
# mkdir -p "$FRECKLES_VIRTUALENV"
# cd "$FRECKLES_VIRTUALENV"
# virtualenv freckles

# install freckles & requirements
# source freckles/bin/activate
# pip install --upgrade pip
# pip install --upgrade setuptools wheel
