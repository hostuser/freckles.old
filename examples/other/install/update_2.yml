---
# a very simple exaample of how to update the default package manager cache
runs:
  - tasks:
      - update
      - upgrade
      - install:
         vars:
           packages:
               - emacs
               - htop
      - update

