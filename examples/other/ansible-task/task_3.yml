vars:
  dotfiles:
    - base_dir: ~/dotfiles
      paths: ["minimal", "dev", "no-x", "x"]
      remote: https://github.com/makkus/dotfiles-new.git
runs:
  - desc: git checkout dotfiles
    tasks:
      - checkout-dotfiles


