runs:
  - desc: debug msg
    tasks:
      - debug:
          meta:
            # type: ansible_task
            role_name: debug_stuff
          vars:
            msg: "SSSSS"
      - debug:
          meta:
            # type: ansible_task
            role_name: debug_stuff
          vars:
            msg: "TTT"
